OPENAI_API_KEY=your-openai-api-key
# CORS origin for dev
ORIGIN=http://localhost:5173
RPM_API_KEY=your-rpm-api-key
# Optional: Host binding (defaults to 0.0.0.0 for Render, set to 127.0.0.1 for local dev)
# HOST=127.0.0.1
# Optional: Port (defaults to 8888, Render will override with its own PORT)
# PORT=8888