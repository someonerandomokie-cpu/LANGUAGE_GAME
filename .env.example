# Frontend build-time envs (optional)
VITE_BACKEND_URL=http://127.0.0.1:8888
# VITE_OPENAI_KEY=your-openai-api-key

# Server envs
OPENAI_API_KEY=your-openai-api-key
# Optional: dedicate a separate key just for image prompts (OpenAI image endpoint)
# IMAGE_OPENAI_API_KEY=your-openai-api-key
# Replicate token for background generation
REPLICATE_API_TOKEN=your-replicate-api-token
# CORS origin (use * for dev)
ORIGIN=*
# Ready Player Me (or other) API key if used
RPM_API_KEY=your-rpm-api-key